{
  "private": true,
  "scripts": {
    "dev": "nuxi dev",
    "build": "NITRO_PRESET=server nuxi build",
    "start": "PORT=$PORT node .output/server/index.mjs"
  },
  "devDependencies": {
    "nuxt3": "latest"
  }
}
